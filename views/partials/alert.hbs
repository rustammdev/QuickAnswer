<div
  id="modalDelete"
  class="fixed inset-0 hidden flex items-center justify-center bg-gray-800 bg-opacity-50 z-50"
>
  <div class="bg-white p-6 rounded shadow-lg w-80">
    <div class="flex justify-between items-center mb-4">
      <h3 class="text-lg font-semibold">Alert</h3>
      <button
        id="closeButton"
        class="text-gray-600 hover:text-gray-900"
      >&times;</button>
    </div>
    <p class="mb-4">Are you sure you want to delete this event?</p>
    <div class="flex justify-end space-x-2">
      <button
        id="confirmDelete"
        class="bg-red-500 text-white px-4 py-2 rounded"
      >Yes</button>
      <button
        id="cancelDelete"
        class="bg-gray-300 text-gray-700 px-4 py-2 rounded"
      >Cancel</button>
    </div>
  </div>
</div>

{{! <script src="./js/dashboard/delete.js"></script> }}